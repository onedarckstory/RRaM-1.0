using UnityEngine;

public class MyScript : MonoBehaviour
{
    // Объявление переменных

    private void Start()
    {
        // Логика, выполняемая при старте сцены
    }

    private void Update()
    {
        // Логика, выполняемая каждый кадр
    }

    private void OnTriggerEnter(Collider other)
    {
        // Логика, выполняемая при столкновении с другим коллайдером
    }
}
